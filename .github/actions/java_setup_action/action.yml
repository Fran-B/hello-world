name: java_setup
description: Action to checkout repo and setup java

#inputs:
#  build_type:
#    description: 'Build type'
#    default: 'test'
#    required: true
#    type: choice
#    options:
#    - test
#    - release
#  version:
#    description: 'Version number'
#    required: false
#    type: string

env:
  runs:
    using: 'node24'
    main: 'main.js'

runs:
  using: "composite"
  steps:
    #- name: Check out repository code
    #  uses: actions/checkout@v6
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        java-package: jdk
